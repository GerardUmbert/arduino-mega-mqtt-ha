// ===========================================================
// CONFIG.H — configuración real de red (NO se sube al repositorio,
// está en .gitignore). Rellena los valores abajo.
// ===========================================================

#ifndef CONFIG_H
#define CONFIG_H

// IP de tu Home Assistant / servidor Mosquitto.
#define BROKER_ADDR IPAddress(0, 0, 0, 0)

// Usuario/contraseña del broker MQTT.
#define MQTT_USER ""
#define MQTT_PASS ""

#endif
